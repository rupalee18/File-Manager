<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= fileName %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-black text-zinc-100 min-h-screen p-6 flex flex-col items-center">
    <!-- Header Section -->
    <div class="mb-8">
        <h1 class="text-4xl font-bold mb-4 text-left"><%= fileName %></h1>
        <a href="/" class="text-blue-400 hover:text-blue-600 transition duration-300 text-lg text-left block mb-4">
            &#8592; Back to Home
        </a>
    </div>

    <!-- File Content Box -->
    <div class="bg-zinc-800 rounded-xl shadow-lg w-full max-w-3xl p-6">
        <h2 class="text-2xl font-semibold mb-4">File Content:</h2>
        <pre class="whitespace-pre-wrap text-lg text-zinc-200"><%= data %></pre>
    </div>
</body>
</html>
